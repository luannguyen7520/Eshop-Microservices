services:
  catalogdb:
    image: postgres
    shm_size: 128mb

  basketdb:
    image: postgres
    shm_size: 128mb

  catalog.api:
    image: ${DOCKER_REGISTRY-}catalogapi
    build:
      context: .
      dockerfile: Services/Catalog/Catalog.API/Dockerfile

